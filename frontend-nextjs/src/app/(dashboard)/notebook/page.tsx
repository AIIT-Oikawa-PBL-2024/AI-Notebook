"use client";

import { NotebookForm } from "@/features/dashboard/notebook/components/NotebookForm";
import type { NextPage } from "next";

const NotebookPage: NextPage = () => {
	return <NotebookForm />;
};

export default NotebookPage as React.ComponentType;
